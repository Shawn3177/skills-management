# Skills Manage Desktop

Tauri 2 + React + TypeScript desktop app for managing local Agent Skills.

## Local Commands

```powershell
npm install
npm run dev
npm run tauri dev
npm test -- --run
npm run build
npm run tauri build
npm run package:portable
```

Rust backend checks:

```powershell
cd src-tauri
cargo test
cargo check
```
